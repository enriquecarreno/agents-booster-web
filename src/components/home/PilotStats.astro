---
/**
 * PILOT STATS SECTION - PREMIUM REDESIGN
 * Stats con animaciones y diseño elegante
 */

import StatsCounter from './StatsCounter';
---

<section class="pilot-stats section">
  <div class="ab-container">
    <div class="stats-header">
      <div class="badge badge-gold">
        <span>🧪</span>
        <span>Programa Piloto</span>
      </div>
      <h2 class="stats-title">
        Resultados <span class="text-gradient-gold">reales</span> de empresas <span class="text-gradient-gold">reales</span>
      </h2>
      <p class="stats-subtitle">
        8 empresas LATAM (retail, e-commerce, servicios) probaron nuestros agentes durante 3 meses. 
        Estos son los números.
      </p>
    </div>

    <div class="stats-grid">
      <div class="stat-item">
        <div class="stat-content">
          <div class="stat-icon-wrapper">
            <div class="stat-icon">🤖</div>
          </div>
          <StatsCounter
            end={5}
            suffix=" Agentes"
            label="especializados listos"
            duration={2000}
            client:visible
          />
        </div>
      </div>

      <div class="stat-item">
        <div class="stat-content">
          <div class="stat-icon-wrapper">
            <div class="stat-icon">💬</div>
          </div>
          <StatsCounter
            end={50}
            suffix="K+"
            label="mensajes procesados"
            duration={2500}
            client:visible
          />
        </div>
      </div>

      <div class="stat-item">
        <div class="stat-content">
          <div class="stat-icon-wrapper">
            <div class="stat-icon">🎯</div>
          </div>
          <StatsCounter
            end={94}
            suffix="%"
            label="precisión vs humano"
            duration={2000}
            client:visible
          />
        </div>
      </div>

      <div class="stat-item">
        <div class="stat-content">
          <div class="stat-icon-wrapper">
            <div class="stat-icon">⚡</div>
          </div>
          <StatsCounter
            end={2.8}
            prefix="<"
            suffix="s"
            decimals={1}
            label="tiempo de respuesta"
            duration={2000}
            client:visible
          />
        </div>
      </div>
    </div>

    <div class="stats-footer">
      <p class="disclaimer">
        📊 Métricas verificadas de programa piloto (Sep-Nov 2024) con 8 empresas en México, Colombia y Argentina
      </p>
    </div>
  </div>
</section>

<style>
  .pilot-stats {
    background: var(--color-bg-elevated);
    position: relative;
    overflow: hidden;
  }

  .pilot-stats::before {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 800px;
    height: 800px;
    background: radial-gradient(circle, rgba(255, 184, 0, 0.05) 0%, transparent 70%);
    pointer-events: none;
  }

  .stats-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto 64px;
  }

  .stats-header .badge {
    margin-bottom: 24px;
  }

  .stats-title {
    font-size: clamp(32px, 4vw, 48px);
    font-weight: 900;
    line-height: 1.2;
    margin-bottom: 16px;
  }

  .stats-subtitle {
    font-size: 18px;
    line-height: 1.7;
    color: rgba(255, 255, 255, 0.7);
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
    position: relative;
    z-index: 1;
  }

  .stat-item {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 20px;
    padding: 32px 24px;
    text-align: center;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  .stat-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at center, rgba(255, 184, 0, 0.05) 0%, transparent 70%);
    opacity: 0;
    transition: opacity 0.4s ease;
  }

  .stat-item:hover::before {
    opacity: 1;
  }

  .stat-item:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(255, 184, 0, 0.3);
    transform: translateY(-8px) scale(1.02);
    box-shadow: 
      0 16px 40px rgba(0, 0, 0, 0.3),
      0 0 60px rgba(255, 184, 0, 0.15);
  }

  .stat-content {
    position: relative;
    z-index: 1;
  }

  .stat-icon-wrapper {
    margin-bottom: 20px;
    display: inline-block;
  }

  .stat-icon {
    font-size: 48px;
    filter: drop-shadow(0 0 20px rgba(255, 184, 0, 0.3));
    animation: float 3s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-8px);
    }
  }

  .stat-item:nth-child(2) .stat-icon {
    animation-delay: 0.5s;
  }

  .stat-item:nth-child(3) .stat-icon {
    animation-delay: 1s;
  }

  .stat-item:nth-child(4) .stat-icon {
    animation-delay: 1.5s;
  }

  .stats-footer {
    margin-top: 48px;
    text-align: center;
  }

  .disclaimer {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.5);
    max-width: 700px;
    margin: 0 auto;
    line-height: 1.6;
  }

  @media (max-width: 1024px) {
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .stats-grid {
      grid-template-columns: 1fr;
      gap: 16px;
    }

    .stat-item {
      padding: 24px 20px;
    }

    .stat-icon {
      font-size: 40px;
    }

    .stats-footer {
      margin-top: 32px;
    }
  }
</style>
