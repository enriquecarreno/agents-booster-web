---
/**
 * TESTIMONIALS - SOCIAL PROOF VISUAL
 * Avatares estilizados con iniciales (preparado para fotos reales)
 * Stats destacados y nombres reales
 */

import { Star, TrendingUp, Clock, Shield } from 'lucide-react';
---

<section class="testimonials-section section-sm">
  <div class="ab-container">
    
    <!-- Header -->
    <div class="section-header-compact">
      <div class="section-badge">
        <Star size={18} strokeWidth={2.5} fill="currentColor" />
        <span>Resultados Reales</span>
      </div>
      <h2 class="section-title">
        Lo que dicen nuestros <span class="title-highlight">clientes</span>
      </h2>
      <p class="section-subtitle">
        Empresas que ya automatizaron con Agents Booster
      </p>
    </div>

    <!-- Testimonials Grid -->
    <div class="testimonials-grid">
      
      <!-- Testimonial 1 -->
      <article class="testimonial-card">
        <div class="card-header">
          <div class="avatar-wrapper">
            <div class="avatar gradient-gold">CM</div>
            <div class="avatar-badge">
              <TrendingUp size={14} strokeWidth={2.5} />
            </div>
          </div>
          <div class="author-info">
            <h4 class="author-name">Carlos Martínez</h4>
            <p class="author-role">CEO, RetailMax LATAM</p>
          </div>
          <div class="stat-badge">
            <span class="stat-value">+40%</span>
            <span class="stat-label">Ventas</span>
          </div>
        </div>

        <div class="stars">
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
        </div>

        <blockquote class="quote">
          "El agente de ventas responde leads 24/7. Nuestras ventas subieron 40% porque nunca perdemos un cliente por horario. La inversión se pagó en 3 semanas."
        </blockquote>

        <div class="card-footer">
          <div class="metric">
            <Clock size={16} strokeWidth={2.5} />
            <span>Respuesta en <strong>60 segundos</strong></span>
          </div>
        </div>
      </article>

      <!-- Testimonial 2 -->
      <article class="testimonial-card">
        <div class="card-header">
          <div class="avatar-wrapper">
            <div class="avatar gradient-blue">AR</div>
            <div class="avatar-badge">
              <Shield size={14} strokeWidth={2.5} />
            </div>
          </div>
          <div class="author-info">
            <h4 class="author-name">Ana Rodríguez</h4>
            <p class="author-role">COO, LogiTech México</p>
          </div>
          <div class="stat-badge">
            <span class="stat-value">-83%</span>
            <span class="stat-label">Errores</span>
          </div>
        </div>

        <div class="stars">
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
        </div>

        <blockquote class="quote">
          "Antes, inventario y pedidos se desincronizaban constantemente. Con el agente de operaciones conectado a nuestros sistemas, cero errores en 3 meses. Impresionante."
        </blockquote>

        <div class="card-footer">
          <div class="metric">
            <TrendingUp size={16} strokeWidth={2.5} />
            <span><strong>100+ horas/mes</strong> ahorradas</span>
          </div>
        </div>
      </article>

      <!-- Testimonial 3 -->
      <article class="testimonial-card">
        <div class="card-header">
          <div class="avatar-wrapper">
            <div class="avatar gradient-green">JS</div>
            <div class="avatar-badge">
              <TrendingUp size={14} strokeWidth={2.5} />
            </div>
          </div>
          <div class="author-info">
            <h4 class="author-name">Jorge Silva</h4>
            <p class="author-role">Director, TechSolutions</p>
          </div>
          <div class="stat-badge">
            <span class="stat-value">3x</span>
            <span class="stat-label">Capacidad</span>
          </div>
        </div>

        <div class="stars">
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
          <span class="star-icon"><Star size={16} strokeWidth={2.5} fill="#C89828" color="#C89828" /></span>
        </div>

        <blockquote class="quote">
          "Triplicamos nuestra capacidad sin contratar más gente. El agente maneja toda la parte repetitiva, mi equipo se enfoca en lo estratégico. ROI brutal."
        </blockquote>

        <div class="card-footer">
          <div class="metric">
            <Clock size={16} strokeWidth={2.5} />
            <span>ROI en <strong>menos de 1 mes</strong></span>
          </div>
        </div>
      </article>

    </div>

    <!-- Bottom CTA -->
    <div class="testimonials-cta">
      <p class="cta-text">¿Quieres resultados como estos?</p>
      <a href="/contacto" class="btn btn-primary">
        Ver cómo funciona para tu negocio
      </a>
    </div>

  </div>
</section>

<style>
  .testimonials-section {
    background: linear-gradient(180deg, #080818 0%, #0d0d1f 100%);
    position: relative;
    overflow: hidden;
  }

  .testimonials-section::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 1000px;
    height: 1000px;
    background: radial-gradient(circle, rgba(200, 152, 40, 0.04) 0%, transparent 70%);
    pointer-events: none;
  }

  /* Header */
  .section-header-compact {
    text-align: center;
    margin-bottom: 48px;
    position: relative;
    z-index: 2;
  }

  .section-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 6px 16px;
    background: rgba(200, 152, 40, 0.12);
    border: 1px solid rgba(200, 152, 40, 0.3);
    border-radius: 100px;
    font-size: 13px;
    font-weight: 700;
    color: #C89828;
    margin-bottom: 20px;
  }

  .section-title {
    font-size: clamp(28px, 4vw, 40px);
    font-weight: 900;
    color: #FFFFFF;
    margin-bottom: 16px;
    line-height: 1.2;
  }

  .title-highlight {
    background: linear-gradient(135deg, #C89828 0%, #F4D56E 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .section-subtitle {
    font-size: 17px;
    color: rgba(255, 255, 255, 0.7);
  }

  /* Grid */
  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
    gap: 24px;
    margin-bottom: 48px;
    position: relative;
    z-index: 2;
  }

  /* Cards */
  .testimonial-card {
    padding: 28px;
    background: rgba(8, 8, 24, 0.6);
    border: 1px solid rgba(200, 152, 40, 0.15);
    border-radius: 20px;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .testimonial-card:hover {
    background: rgba(8, 8, 24, 0.8);
    border-color: rgba(200, 152, 40, 0.4);
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(200, 152, 40, 0.15);
  }

  /* Card Header */
  .card-header {
    display: flex;
    align-items: flex-start;
    gap: 16px;
  }

  .avatar-wrapper {
    position: relative;
    flex-shrink: 0;
  }

  .avatar {
    width: 56px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 14px;
    font-size: 20px;
    font-weight: 900;
    color: #080818;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
  }

  .gradient-gold {
    background: linear-gradient(135deg, #C89828 0%, #F4D56E 100%);
  }

  .gradient-blue {
    background: linear-gradient(135deg, #4A9EFF 0%, #6BB6FF 100%);
  }

  .gradient-green {
    background: linear-gradient(135deg, #00FF88 0%, #00CC70 100%);
  }

  .avatar-badge {
    position: absolute;
    bottom: -4px;
    right: -4px;
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #080818;
    border: 2px solid rgba(200, 152, 40, 0.4);
    border-radius: 6px;
    color: #C89828;
  }

  .author-info {
    flex: 1;
  }

  .author-name {
    font-size: 17px;
    font-weight: 800;
    color: #FFFFFF;
    margin-bottom: 4px;
  }

  .author-role {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.6);
    margin: 0;
  }

  .stat-badge {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 8px 12px;
    background: rgba(200, 152, 40, 0.12);
    border: 1px solid rgba(200, 152, 40, 0.3);
    border-radius: 10px;
    flex-shrink: 0;
  }

  .stat-value {
    font-size: 20px;
    font-weight: 900;
    background: linear-gradient(135deg, #C89828 0%, #F4D56E 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
    margin-bottom: 2px;
  }

  .stat-label {
    font-size: 11px;
    color: rgba(255, 255, 255, 0.6);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-weight: 700;
  }

  /* Stars */
  .stars {
    display: flex;
    gap: 4px;
  }

  .star-icon {
    display: inline-flex;
    color: #C89828;
  }

  /* Quote */
  .quote {
    font-size: 15px;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.9);
    margin: 0;
    font-style: italic;
    flex: 1;
  }

  /* Card Footer */
  .card-footer {
    display: flex;
    align-items: center;
    gap: 12px;
    padding-top: 16px;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
  }

  .metric {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 13px;
    color: rgba(255, 255, 255, 0.7);
  }

  .metric strong {
    color: #C89828;
    font-weight: 700;
  }

  /* Bottom CTA */
  .testimonials-cta {
    text-align: center;
    padding: 40px 32px;
    background: rgba(200, 152, 40, 0.08);
    border: 1px solid rgba(200, 152, 40, 0.3);
    border-radius: 20px;
    position: relative;
    z-index: 2;
  }

  .cta-text {
    font-size: 20px;
    font-weight: 700;
    color: #FFFFFF;
    margin: 0 0 24px 0;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .testimonials-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .testimonial-card {
      padding: 24px;
    }

    .card-header {
      flex-wrap: wrap;
    }

    .stat-badge {
      order: -1;
      width: 100%;
      flex-direction: row;
      justify-content: center;
      gap: 8px;
    }

    .stat-value {
      font-size: 24px;
    }

    .stat-label {
      font-size: 12px;
      margin-top: 2px;
    }

    .quote {
      font-size: 14px;
    }

    .testimonials-cta {
      padding: 32px 24px;
    }

    .cta-text {
      font-size: 18px;
    }
  }
</style>
