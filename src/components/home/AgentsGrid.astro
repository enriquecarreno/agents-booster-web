---
/**
 * AGENTS GRID V2.1 - SPRINT 2
 * Copy Rewrite: Más enfoque en lo que cada agente te resuelve
 */

import { SALES_AGENT, OPERATIONS_AGENT, MARKETING_AGENT, HR_AGENT, ASSISTANT_AGENT, CUSTOM_AGENT } from '../../lib/agents-data';
import { baseUrl } from '../../lib/base-url';
import { Bot, Rocket, Lightbulb, Link2, ArrowRight, Bell } from 'lucide-react';

const agents = [
  { ...SALES_AGENT, slug: 'vendedor' },
  { ...OPERATIONS_AGENT, slug: 'operaciones' },
  { ...MARKETING_AGENT, slug: 'marketing' },
  { ...HR_AGENT, slug: 'rrhh' },
  { ...ASSISTANT_AGENT, slug: 'asistente' },
  { ...CUSTOM_AGENT, slug: 'personalizado' },
];
---

<section class="agents-grid section">
  <div class="ab-container">
    
    <!-- Header -->
    <div class="agents-header">
      <div class="agents-badge">
        <Bot size={18} strokeWidth={2.5} className="badge-icon-lucide" client:load />
        <span>6 Agentes Especializados</span>
      </div>
      <h2 class="agents-title">
        Un agente para <span class="title-highlight">cada problema</span>
      </h2>
      <p class="agents-subtitle">
        Cada agente domina su área y se conecta a tus sistemas reales. Activa el que necesitas o combina varios para automatizar toda tu operación.
      </p>
    </div>

    <!-- Grid -->
    <div class="grid">
      {agents.map((agent) => (
        <article class="agent-card" data-status={agent.available ? 'available' : 'beta'}>
          
          <!-- Status Badge -->
          <div class="card-header">
            {agent.available ? (
              <span class="status-badge available">
                <span class="badge-dot"></span>
                <span>Disponible</span>
              </span>
            ) : (
              <span class="status-badge beta">
                <Rocket size={14} strokeWidth={2.5} className="badge-icon-lucide-beta" client:load />
                <span>Beta</span>
              </span>
            )}
          </div>

          <!-- Icon -->
          <div class="agent-icon">{agent.icon}</div>

          <!-- Title -->
          <h3 class="agent-name">{agent.name}</h3>

          <!-- Tagline -->
          <p class="agent-tagline">{agent.tagline}</p>

          <!-- Description -->
          <p class="agent-description">{agent.description}</p>

          <!-- Metrics -->
          {agent.metrics && agent.metrics.length > 0 && (
            <div class="agent-metrics">
              {agent.metrics.map((metric) => (
                <div class="metric-item">
                  <div class="metric-value">{metric.value}</div>
                  <div class="metric-label">{metric.label}</div>
                </div>
              ))}
            </div>
          )}

          <!-- CTA -->
          {agent.available ? (
            <a href={`${baseUrl}/agentes/${agent.slug}`} class="agent-cta">
              <span>Ver detalles</span>
              <ArrowRight size={18} strokeWidth={2.5} className="cta-arrow-lucide" client:load />
            </a>
          ) : (
            <button class="agent-cta beta-cta" disabled>
              <span>Próximamente</span>
              <Bell size={18} strokeWidth={2.5} className="cta-icon-lucide" client:load />
            </button>
          )}

        </article>
      ))}
    </div>

    <!-- Bottom Info -->
    <div class="agents-info">
      <div class="info-card">
        <div class="info-icon-wrapper">
          <Lightbulb size={48} strokeWidth={2} className="info-icon-lucide" client:load />
        </div>
        <div class="info-content">
          <h4 class="info-title">¿Necesitas algo específico?</h4>
          <p class="info-text">
            Construimos agentes personalizados para procesos únicos de tu industria. Si tienes un flujo complejo, lo automatizamos.
          </p>
          <a href={`${baseUrl}/agentes/personalizado`} class="info-link">
            Conocer Agente Personalizado
            <ArrowRight size={16} strokeWidth={2.5} client:load />
          </a>
        </div>
      </div>

      <div class="info-card">
        <div class="info-icon-wrapper">
          <Link2 size={48} strokeWidth={2} className="info-icon-lucide" client:load />
        </div>
        <div class="info-content">
          <h4 class="info-title">Revenue Duo (Bundle)</h4>
          <p class="info-text">
            Ventas + Operaciones trabajando juntos. Vendes más rápido y entregas sin errores. 15% de descuento al combinarlos.
          </p>
          <a href={`${baseUrl}/planes`} class="info-link">
            Ver Revenue Duo
            <ArrowRight size={16} strokeWidth={2.5} client:load />
          </a>
        </div>
      </div>
    </div>

  </div>
</section>

<style>
  .agents-grid {
    background: #080818;
    position: relative;
    overflow: hidden;
  }

  .agents-grid::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 1200px;
    height: 1200px;
    background: radial-gradient(circle, rgba(200, 152, 40, 0.06) 0%, transparent 70%);
    pointer-events: none;
  }

  /* Header */
  .agents-header {
    text-align: center;
    margin-bottom: 48px;
    position: relative;
    z-index: 2;
  }

  .agents-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 20px;
    background: rgba(200, 152, 40, 0.12);
    border: 1px solid rgba(200, 152, 40, 0.3);
    border-radius: 100px;
    font-size: 14px;
    font-weight: 700;
    color: #C89828;
    margin-bottom: 24px;
  }

  .agents-badge :global(.badge-icon-lucide) {
    color: #C89828;
    flex-shrink: 0;
  }

  .agents-title {
    font-size: clamp(32px, 5vw, 48px);
    font-weight: 900;
    color: #FFFFFF;
    margin-bottom: 20px;
    line-height: 1.2;
  }

  .title-highlight {
    background: linear-gradient(135deg, #C89828 0%, #F4D56E 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .agents-subtitle {
    font-size: 18px;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.7);
    max-width: 800px;
    margin: 0 auto;
  }

  /* Grid */
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
    gap: 24px;
    margin-bottom: 48px;
    position: relative;
    z-index: 2;
  }

  .agent-card {
    padding: 28px;
    background: rgba(8, 8, 24, 0.6);
    border: 1px solid rgba(200, 152, 40, 0.15);
    border-radius: 20px;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    flex-direction: column;
  }

  .agent-card[data-status="available"]:hover {
    background: rgba(8, 8, 24, 0.8);
    border-color: rgba(200, 152, 40, 0.4);
    transform: translateY(-4px);
    box-shadow: 0 8px 32px rgba(200, 152, 40, 0.15);
  }

  .agent-card[data-status="beta"] {
    opacity: 0.85;
  }

  .agent-card[data-status="beta"]:hover {
    opacity: 1;
    border-color: rgba(74, 158, 255, 0.3);
  }

  /* Card Header */
  .card-header {
    display: flex;
    justify-content: flex-end;
    margin-bottom: 16px;
  }

  .status-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 6px 12px;
    border-radius: 100px;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .status-badge.available {
    background: rgba(0, 255, 136, 0.15);
    border: 1px solid rgba(0, 255, 136, 0.3);
    color: #00FF88;
  }

  .badge-dot {
    width: 6px;
    height: 6px;
    background: #00FF88;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% {
      opacity: 1;
      box-shadow: 0 0 0 0 rgba(0, 255, 136, 0.7);
    }
    50% {
      opacity: 0.8;
      box-shadow: 0 0 0 6px rgba(0, 255, 136, 0);
    }
  }

  .status-badge.beta {
    background: rgba(74, 158, 255, 0.15);
    border: 1px solid rgba(74, 158, 255, 0.3);
    color: #4A9EFF;
  }

  .status-badge.beta :global(.badge-icon-lucide-beta) {
    color: #4A9EFF;
    flex-shrink: 0;
  }

  /* Icon */
  .agent-icon {
    font-size: 64px;
    margin-bottom: 20px;
    text-align: center;
    filter: drop-shadow(0 4px 8px rgba(200, 152, 40, 0.3));
  }

  /* Content */
  .agent-name {
    font-size: 24px;
    font-weight: 800;
    color: #FFFFFF;
    margin-bottom: 12px;
    text-align: center;
  }

  .agent-tagline {
    font-size: 15px;
    font-weight: 600;
    color: #C89828;
    margin-bottom: 16px;
    text-align: center;
  }

  .agent-description {
    font-size: 15px;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 24px;
    flex: 1;
    text-align: center;
  }

  /* Metrics */
  .agent-metrics {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    margin-bottom: 24px;
    padding: 20px;
    background: rgba(200, 152, 40, 0.05);
    border: 1px solid rgba(200, 152, 40, 0.1);
    border-radius: 12px;
  }

  .metric-item {
    text-align: center;
  }

  .metric-value {
    font-size: 24px;
    font-weight: 900;
    background: linear-gradient(135deg, #C89828 0%, #F4D56E 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
    margin-bottom: 6px;
  }

  .metric-label {
    font-size: 11px;
    color: rgba(255, 255, 255, 0.6);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-weight: 600;
  }

  /* CTA */
  .agent-cta {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    width: 100%;
    padding: 14px 24px;
    font-size: 15px;
    font-weight: 700;
    border-radius: 12px;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    border: none;
    cursor: pointer;
    background: linear-gradient(135deg, #C89828 0%, #E0B84E 100%);
    color: #080818;
    box-shadow: 0 4px 12px rgba(200, 152, 40, 0.3);
  }

  .agent-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(200, 152, 40, 0.5);
    color: #080818;
  }

  .agent-cta :global(.cta-arrow-lucide) {
    color: #080818;
    transition: transform 0.3s ease;
    flex-shrink: 0;
  }

  .agent-cta:hover :global(.cta-arrow-lucide) {
    transform: translateX(4px);
  }

  .agent-cta.beta-cta {
    background: rgba(74, 158, 255, 0.15);
    border: 2px solid rgba(74, 158, 255, 0.3);
    color: #4A9EFF;
    cursor: not-allowed;
  }

  .agent-cta.beta-cta:hover {
    transform: none;
    box-shadow: none;
  }

  .agent-cta.beta-cta :global(.cta-icon-lucide) {
    color: #4A9EFF;
    flex-shrink: 0;
  }

  /* Bottom Info */
  .agents-info {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: 24px;
    position: relative;
    z-index: 2;
  }

  .info-card {
    display: flex;
    gap: 20px;
    padding: 28px;
    background: rgba(200, 152, 40, 0.08);
    border: 1px solid rgba(200, 152, 40, 0.2);
    border-radius: 20px;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .info-card:hover {
    background: rgba(200, 152, 40, 0.12);
    border-color: rgba(200, 152, 40, 0.3);
    transform: translateY(-2px);
  }

  .info-icon-wrapper {
    flex-shrink: 0;
  }

  .info-icon-wrapper :global(.info-icon-lucide) {
    color: #C89828;
    filter: drop-shadow(0 2px 8px rgba(200, 152, 40, 0.3));
  }

  .info-content {
    flex: 1;
  }

  .info-title {
    font-size: 18px;
    font-weight: 700;
    color: #FFFFFF;
    margin-bottom: 8px;
  }

  .info-text {
    font-size: 15px;
    line-height: 1.5;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 12px;
  }

  .info-link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 15px;
    font-weight: 700;
    color: #C89828;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .info-link:hover {
    color: #E0B84E;
    transform: translateX(4px);
  }

  .info-link :global(svg) {
    color: #C89828;
    flex-shrink: 0;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .grid {
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    }

    .agents-info {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .agents-header {
      margin-bottom: 48px;
    }

    .grid {
      grid-template-columns: 1fr;
      gap: 20px;
      margin-bottom: 48px;
    }

    .agent-card {
      padding: 28px 24px;
    }

    .agent-icon {
      font-size: 56px;
    }

    .agent-name {
      font-size: 22px;
    }

    .agent-metrics {
      gap: 12px;
      padding: 16px;
    }

    .metric-value {
      font-size: 20px;
    }

    .info-card {
      padding: 24px;
      flex-direction: column;
      align-items: center;
      text-align: center;
    }

    .info-icon-wrapper :global(.info-icon-lucide) {
      width: 40px;
      height: 40px;
    }
  }
</style>
